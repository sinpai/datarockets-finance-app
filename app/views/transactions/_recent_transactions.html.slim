.col-md-8.offset-md-2
  table.table.table-striped.table-hover
    thead
      tr
        th[scope="col"]= t('.date')
        th= t('.amount')
        th= t('.comment')
    tbody
    - @recent_records.each do |record|
      tr
        th.[scope="row"]= record.transactinable.date
        th= record.amount
        th= record.transactinable.comment
